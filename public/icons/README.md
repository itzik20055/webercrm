# Icons

`icon-192.png` and `icon-512.png` are the PNG icons used by the PWA manifest.

For a quick start, you can convert `icon-192.svg` to PNG at 192x192 and 512x512.

Until then, the SVG fallback `icon-192.svg` is bundled.
